# 知識庫健康檢查報告 — 2026-04-30

> 本次是內容增量 + hygiene pass 的混合：補上 2026-04-25 health 列為 P3 的三個 kernel 主題（rcu / bitmap / workqueue）、把 2026-04-25–04-30 之間新增的兩份大素材（MCUPM_Code_Review、google_stack_with_min）正式編入 wiki，並同步索引、統計與健康檢查連結。

---

## 1. 總覽

| 指標 | 上次 (2026-04-25) | 本次 (2026-04-30) | 變化 |
|------|---------------------|---------------------|------|
| Wiki markdown 檔案 | 44 | 58 | +14（kernel 3、interview 2、health 1、log 1、上次低估的 7 份 interview 檔本次入帳）|
| Wiki 總行數 | 16,139 | 20,700 (~20,447 + 本檔 + log) | +約 4,560 行 |
| Wiki patterns 檔案 | 19 | 19 | — |
| Wiki c-idioms 檔案 | 4 | 4 | — |
| Wiki kernel 檔案 | 9 | **12** | +3（rcu, bitmap, workqueue）|
| Wiki interview 檔案 | 6 | **15** | +2 本次（mcupm-code-review, google-stack-with-min）；+7 上次低估 |
| 最新索引 | `_index.md` | 已同步至 2026-04-30 | ✅ |
| 最新 overview | `overview.md` | 已同步至 2026-04-30 | ✅ |

> **註**：2026-04-25 health 報告當時記為 「Wiki interview 檔案 6」，但實際上 wiki/interview/ 在 2026-04-26 ~ 04-29 之間已陸續加入 7 份檔（4 份 code-review-solution + 3 份 google_code_review_semaphore_prep_linux*）。本次盤點才把這 7 份補進統計，所以「interview 6 → 15」中 +9 是「2 新寫 + 7 補登」，而非 9 份全新。

---

## 2. 本次新增與修正

### 2.1 新增 wiki/kernel/ 三主題（接續 2026-04-25 P3 清單）

- **`wiki/kernel/rcu.md`**（約 200 行）— Read-Copy-Update 完整介紹：read/update/grace period 三層 API、list/hlist 變體、SRCU/Tasks RCU 變體、三大陷阱、與其他原語對比、源碼定位、面試速記。
- **`wiki/kernel/bitmap.md`**（約 200 行）— 位元圖：DECLARE_BITMAP、單 bit 操作（atomic vs `__` 版）、批次掃描（`find_first_zero_bit` 邊界判斷）、整段操作、三個 idiomatic 用法（資源池 / try-claim / cpumask）、陷阱、源碼定位。
- **`wiki/kernel/workqueue.md`**（約 220 行）— 工作佇列：兩個層次的 API、`alloc_workqueue` flag 表（取代 deprecated `create_workqueue`）、delayed_work、四個必背同步 API、teardown 三步、與其他 deferred 機制對比。

### 2.2 新增 wiki/interview/ 兩份索引

- **`wiki/interview/mcupm-code-review.md`**（約 180 行）— 為 `MCUPM_Code_Review/` 兩份大文件（Q + A 共 1,612 行）建立 wiki 索引：11 題對照表（每題對應的 wiki 章節）、bug 嚴重度三層分類、11 條 review checklist、三條學習路徑（lifecycle / concurrency / power）。
- **`wiki/interview/google-stack-with-min.md`**（約 180 行）— 為 `interview/google_stack_with_min/` 八件套（1,601 行）建立 wiki 索引：5 個 bug 對照表、八件套角色說明、`#ifndef GOOGLE_STACK_WITH_MIN_NO_MAIN` 銜接技巧、四層偵測方法論、O(1) `min` 進階追問。

### 2.3 `_index.md` 同步

- 目錄樹：kernel `9 → 12`、interview `6 → 15`。
- Kernel 區段新增「Lockless 與 deferred work（2026-04-30 新增）」子節，列入 rcu / bitmap / workqueue 三檔的核心 bullet。
- Interview 區段新增「Driver-level / 教材級 code review（2026-04-30 新增）」子節，列入 mcupm-code-review / google-stack-with-min。
- 「快速查找：問題 → 知識映射」新增 rcu / bitmap / workqueue 三條目。
- 統計表更新：總計 `51 / 18,100` → `58 / 20,700`。
- 「最近一次 lint」連結改 `2026-04-30`。
- README 末段「進階主題（未涵蓋 / TODO）」把 RCU / Bitmap / Work Queue 三條畫上刪除線並標 ✅，新增 Per-CPU 變數、dm-verity / fsverity 兩個未來候選。

### 2.4 `overview.md` 同步

- 頂部摘要：Kernel `9 → 12 子系統`，新增 「15 份 interview 教材」字樣。
- §3 表格新增 rcu / bitmap / workqueue 三列。
- §4 從「4 份 cheat sheet」擴充為「15 份教材，分四群」（通用 cheat sheet / weekly mock / 同步原語系列 / driver-level）並加閱讀順序建議。
- §5 Week 2 加 spinlock + rcu 延伸；Week 4 加 bitmap 延伸；Week 5-6 加 workqueue + 兩份 driver review 終局練習。
- §7 統計表全面更新；最後 lint 日期改 `2026-04-30`。

### 2.5 新增日誌與健康檢查

- 本檔 `wiki/health/2026-04-30_lint.md`。
- `wiki/logs/2026-04-30_session.md`。

---

## 3. 仍待處理

### P2：Raw 側清理（延續）

- `206-reverse-linked-list/` 與 `206_Reverse Linked List/` 仍是重複目錄候選。
- `700-search-in-a-binary-search-tree/` 與 `700_Search in a Binary Search Tree/` 仍是重複目錄候選。

需要先比較兩份內容完整度才能合併。本次未動。

### P2：Companion 檔補齊

延續 2026-04-18 / 2026-04-25 lint 的 raw 側缺口。本次新增的觀察：

- **146 LRU Cache** 已於 2026-04-27 補完 `_Memory.md` / `_Linux.md` / `_Opus-4.6.md`，✅ 完整。
- **287 Find the Duplicate Number** 已於 Mar 29 補完三件套，✅ 完整；2026-04-28 新增 `solution.c` 是練習。
- **141 Linked List Cycle** 已完整三件套；2026-04-28 新增 `141_test.c` 是練習。
- **232 Implement Queue using Stacks** 仍只有單一 `.c`，缺 `_Memory.md` / `_Linux.md` / `_Opus-4.6.md`。建議優先補（與 [[kernel/circular-buffer]] 高度相關）。

### P3：Kernel 進階主題（更新 backlog）

2026-04-25 列的三題本次全部完成。新的進階主題候選：

- `kernel/per-cpu.md` — `percpu_counter`、`__this_cpu_*`、`local_lock`；scheduler / mm / network 大量使用。
- `kernel/refcount.md` — `refcount_t` vs `atomic_t`、underflow / overflow detection、與 RCU / list 的搭配。
- `kernel/completion.md` — `struct completion`、`wait_for_completion[_interruptible_timeout]`；雖 `[[interview/google_code_review_semaphore_prep_linux_primitives]]` 已涵蓋概念，未獨立成 kernel 主題。

### P3：跨 wiki 雙向連結審計（新增）

新增的五份檔已主動寫入大量 `[[...]]` 連結，但對端 wiki 還沒回連。下次 hygiene pass 時可：

- 在 `[[kernel/spinlock]]` 末段「相關 wiki」加上 `[[kernel/rcu]]`、`[[kernel/bitmap]]`、`[[kernel/workqueue]]`。
- 在 `[[kernel/list-head]]` / `[[kernel/hash-table]]` 加 `[[kernel/rcu]]` 對照（list / hash 的 RCU 變體）。
- 在 `[[patterns/bit-manipulation]]` 加「kernel 對照」段指向 `[[kernel/bitmap]]`。
- 在 `[[interview/code-review-questions]]` 加 `[[interview/mcupm-code-review]]` 為「driver 級實戰」。

---

## 4. 判斷

### 4.1 內容深度評估

本次新增的 5 份檔（3 kernel + 2 interview）都對齊本知識庫的格式規範：每篇 ~200 行、有「核心概念 / API / 三大陷阱 / 與其他原語對比 / 源碼定位 / 相關 wiki / 面試速記」的標準七段結構。深度上：

- **rcu / bitmap / workqueue** 三檔都直接服務 MCUPM driver review 與 SCMI / DSP IPC code review，不是孤立的理論主題。
- **mcupm-code-review** 補上「driver 級真實 production code review」這一層，原本 wiki 只有教材級（weekly mock / google_stack_with_min）與單一主題（semaphore_prep）；現在三層俱全。
- **google-stack-with-min** 把 SKILL.md 明文指定的 reference 教材正式登錄，未來新增 `interview/<topic>/` 八件套都有對照標準。

### 4.2 維護節奏

從 2026-04-18（首次 lint）→ 04-25（hygiene pass）→ 04-30（內容增量 + hygiene），平均約一週一次。本次特別之處在於：**內容增量比例首次大於 hygiene 比例**。下一次（建議 5 月初）回到單純 hygiene pass，把上面 §3 的雙向連結審計做掉，並評估 raw 側 232 / 重複目錄是否動手清理。

### 4.3 一行總結

本次把 2026-04-25 health 列的 P3 三題（rcu / bitmap / workqueue）全部清掉，並把過去一週累積的兩份大素材（MCUPM driver review、google_stack_with_min 教材）正式編入 wiki；wiki 規模從 51 / 18,100 跳到 58 / 20,700，索引、統計與交叉引用一併同步。
