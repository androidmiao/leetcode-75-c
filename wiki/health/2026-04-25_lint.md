# 知識庫健康檢查報告 — 2026-04-25

> 本次是 wiki hygiene pass：重點不是重寫內容，而是同步索引、統計、交叉引用與前次 lint 後新增的文件狀態。

---

## 1. 總覽

| 指標 | 值 | 狀態 |
|------|-----|------|
| Wiki markdown 檔案 | 44 | 本次新增 health + log 後 |
| Wiki 總行數 | 16,139 | 本次新增 health + log 後 |
| Wiki patterns 檔案 | 19 | 15 核心模式 + 4 補充模式 |
| Wiki c-idioms 檔案 | 4 | 穩定 |
| Wiki kernel 檔案 | 9 | 2026-04-21 已加入 `spinlock.md` |
| Wiki interview 檔案 | 6 | 4 核心 cheat sheet + weekly mock + Mon-3 解答 |
| 最新索引 | `_index.md` | 已同步至 2026-04-25 |
| 最新 overview | `overview.md` | 已同步統計與 lint 日期 |

---

## 2. 本次修正

### 2.1 `_index.md` metadata 同步

- `patterns/` 從舊稱 15 個更新為 19 個。
- `interview/` 從舊稱 4 / 5 份 cheat sheet，統一為 6 份實戰文件。
- 最新健康檢查連結改指向 `health/2026-04-25_lint.md`。
- 日誌區新增 `logs/2026-04-25_session.md`。
- 統計區更新為 44 個 markdown、約 16,100 行。
- Hash Table 快速查找移除重複的題號 `1`。

### 2.2 `overview.md` 同步

- 頂部摘要更新為 19 個演算法模式。
- 統計概覽更新為 patterns 19、kernel 9、interview 6。
- 最新 lint 日期更新為 2026-04-25。
- Week 5-6 的 interview 閱讀提示改為核心 cheat sheet + weekly mock bank。

### 2.3 交叉引用補強

- 在 `patterns/bfs-dfs-graph.md` 的拓撲排序段落加上 `[[topological-sort]]` 參見，完成 2026-04-18 lint 中標記的 TODO。

---

## 3. 仍待處理

### P2：Raw 側清理

- `206-reverse-linked-list/` 與 `206_Reverse Linked List/` 仍是重複目錄候選。
- `700-search-in-a-binary-search-tree/` 與 `700_Search in a Binary Search Tree/` 仍是重複目錄候選。

本次未清理資料夾，因為需要先決定保留哪一份。

### P2：Companion 檔補齊

延續 2026-04-18 lint 的 raw 側缺口：

- 優先補 `_Memory.md` 與 `_Linux.md`，因為這是本 repo 和一般題解最不同的價值。
- 再補 `_Opus-4.6.md`，把單題推理回存成可被 wiki 編譯的素材。

### P3：Kernel 進階主題

仍可考慮新增：

- `kernel/bitmap.md`：承接 `patterns/bit-manipulation.md` 與 SCMI token allocator 的 bitmap 題型。
- `kernel/rcu.md`：承接 hash-table、list-head、spinlock、code review 中大量 RCU 內容。
- `kernel/workqueue.md`：承接 deferred work、SCMI notification、IRQ bottom-half 題型。

---

## 4. 判斷

目前 wiki 的主體內容是健康的；真正需要注意的是「新增專題後同步 metadata」的維護節奏。2026-04-20 firmware、2026-04-21 spinlock 兩次內容增量都很有價值，但讓 2026-04-18 的舊健康報告變成歷史紀錄，而不再適合作為最新狀態。

建議之後每次新增 kernel 或 interview 大主題時，同步做三件事：

1. 更新 `_index.md` 的目錄、統計與最後更新日期。
2. 更新 `overview.md` 的學習地圖與統計概覽。
3. 新增一筆 `logs/YYYY-MM-DD_session.md`，必要時新增 health lint。

---

## Sources

- `wiki/_index.md`
- `wiki/overview.md`
- `wiki/patterns/bfs-dfs-graph.md`
- `wiki/health/2026-04-18_lint.md`
- `wiki/logs/2026-04-18_session.md`
