1_Two Sum.c 已完成，內容是可直接提交到 LeetCode 的 C 解法，採用雜湊表實作，時間複雜度 O(n)、空間複雜度 O(n)，也符合題目的 Follow-up。

我把程式內補成偏教學版寫法：前面有整體解法說明，函式內幾乎每一步都有中文註解，包含為什麼先找 complement、怎麼處理 hash、碰撞時如何線性探測、以及回傳前的記憶體釋放。也已經用 cc 編譯檢查過，語法正常。



我已經把圖解說明直接寫進 1_Two Sum.c 的註解區了。

現在檔案上方有：

陣列索引圖
第 1 輪、第 2 輪的流程圖解
雜湊表概念表格
為什麼 O(n) 比暴力法 O(n^2) 快的說明




不完全一樣，但核心方向一致。

`[1_Two Sum_Solution.md](/Users/alex.miao/Documents/GitHub/leetcode-75-c/1_Two%20Sum/1_Two%20Sum_Solution.md)` 裡面寫了 3 種解法：
1. 暴力法 `O(n^2)`
2. 兩次走訪雜湊表 `O(n)`
3. 一次走訪雜湊表 `O(n)`

我寫在 `[1_Two Sum.c](/Users/alex.miao/Documents/GitHub/leetcode-75-c/1_Two%20Sum/1_Two%20Sum.c)` 的是第 3 種，也就是 `One-pass Hash Table`。流程是掃描陣列時，先查 `complement` 是否已存在；若存在就直接回傳，不存在才把目前值存進表裡。

如果要比較哪個比較好：
- 暴力法最差，因為時間是 `O(n^2)`。
- 兩次走訪雜湊表和一次走訪雜湊表，理論上時間都算 `O(n)`。
- 實務上我寫的這個一次走訪版本通常比較好，因為只掃一遍陣列，流程更精簡，提早找到答案時也能更快返回。

所以結論是：`Solution.md` 裡有包含我寫的解法，而且我寫的是裡面較好的那一種。唯一要補充的是，我的 C 版本是「自己實作雜湊表 + 線性探測」，而 `Solution.md` 是演算法層級說明，沒有綁定特定 C 實作細節。